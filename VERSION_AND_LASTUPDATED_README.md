# Version and Last Updated AI Metadata Update

This update includes all four final Copilot recommendations:

1. `relatedPlaylists` inside each metadata file.
2. `category.json` inside each category folder.
3. `version` added to `playlist-index.json`.
4. `lastUpdated` added to `playlist-index.json`.

The current dataset version is `1.0.0` and the last updated date is `2026-06-12`.

Suggested commit message:

`Add version and last updated fields to playlist index`
